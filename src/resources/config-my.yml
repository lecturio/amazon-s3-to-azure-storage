aws:
  access.key:
  secret.key:
  sqs.region: sqs.eu-west-1.amazonaws.com
  sqs.pool.url:
  sqs.push.url:

azure:
  storage.key: ""
  storage.account:

backups:
  aws.to.azure:
    amazon-bucket: azure-container

local.sourcelist: fullpath/sourcelist
temp.cloud.store: fullpath/assets

trace: true
retries: 5
